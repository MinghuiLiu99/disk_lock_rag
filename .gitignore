# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# 编辑器
.idea/
.vscode/

# Office 临时锁文件
~$*

# 运行期产物
output/index/_app_pid.txt

# ---- 索引缓存：入库与否的分界线 ----
# output/ 下的 nodes.jsonl / check_report.md / figures / manifest.json 是核心资产，必须入库；
# output/index/ 是索引缓存（单本 embeddings.json 约 5MB，混库约 10MB），可由 nodes.jsonl 重建。
output/index/

# ---- 以下内容不入库，需本地自备 ----
# 规范原文（受著作权保护，不适合推送到远端）
规范/

# 本地遗留文件（不入库；如确有内容需要跟踪，用 git add -f）
实验计划.docx
